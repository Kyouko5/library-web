<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kyouko.libraryweb.mapper.BookMapper">

    <resultMap id="BaseResultMap" type="com.kyouko.libraryweb.domain.Book">
            <id property="id" column="id" />
            <result property="title" column="title" />
            <result property="author" column="author" />
            <result property="publisher" column="publisher" />
            <result property="isbn" column="isbn" />
            <result property="createdAt" column="created_at" />
            <result property="updatedAt" column="updated_at" />
            <result property="price" column="price" />
            <result property="publishTime" column="publish_time" />
            <result property="stock" column="stock" />
            <result property="version" column="version" />
    </resultMap>

    <sql id="Base_Column_List">
        id,title,author,
        publisher,isbn,created_at,
        updated_at,price,publish_time,
        stock,version
    </sql>
    <update id="decreaseStock" parameterType="map">
        UPDATE book
        SET stock = stock - 1,
            version = version + 1
        WHERE id = #{id}
          AND version = #{version}
          AND stock > 0
    </update>

    <update id="increaseStock" parameterType="map">
        UPDATE book
        SET stock = stock + 1,
            version = version + 1
        WHERE id = #{id}
          AND version = #{version}
    </update>

</mapper>
